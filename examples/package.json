{
  "name": "tinyfft-examples",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Browser examples for the tinyfft package (built from the local source)",
  "scripts": {
    "dev": "serve -l 3000 .",
    "build:lib": "npm --prefix .. install && npm --prefix .. run build",
    "copy:lib": "mkdir -p lib/tinyfft/dist && cp ../dist/index.js ../dist/tinyfft.wasm lib/tinyfft/dist/",
    "postinstall": "npm run build:lib && npm run copy:lib"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
